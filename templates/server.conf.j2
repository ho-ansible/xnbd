## /etc/xnbd.conf
# Network block device: server
# {{ ansible_managed }}

{
  "wrapper": {
    "address": "{{ xnbd_address }}",
    "port": {{ xnbd_port }},
    "socket": "/var/run/xnbd.ctl",
    "logpath": "/var/log/xnbd.log",
    "max_buf_size": 0,	# https://bitbucket.org/hirofuchi/xnbd/issues/11/
    "max_queue_size": 0,
    "volumes": {
      "0": "{{ xnbd_img }}"
    }
  }
}
